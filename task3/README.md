Task3 - String List Processor
Программа для обработки строк с сортировкой и реверсом.

📋 Что делает
Читает строки из stdin
Разбивает на слова и специальные символы (|, >, && и др.)
Показывает:
Исходный порядок
Отсортированный список
Реверсированный порядок
🛠 Управление
Ввод: Вводите строки для обработки
Выход: Ctrl+D - корректное завершение
Память: Автоматическое управление, проверка через Valgrind
🔧 Описание функций
Функции работы со списком (list.c)
void init_list(StringList *list)
Назначение: Инициализация динамического списка строк
Параметры:

list - указатель на структуру StringList
Действие:
Устанавливает начальную емкость = 10
Выделяет память под массив указателей
Инициализирует size = 0
void add_to_list(StringList *list, const char *str)
Назначение: Добавление строки в список
Параметры:

list - целевой список
str - строка для добавления
Действие:
При необходимости удваивает емкость списка
Выделяет память под копию строки
Добавляет строку в конец списка
void sort_list(StringList *list)
Назначение: Сортировка списка по алфавиту
Алгоритм: Пузырьковая сортировка
Действие:

Сравнивает строки с помощью strcmp()
Меняет местами элементы при необходимости
void reverse_list(StringList *list)
Назначение: Реверс порядка элементов в списке
Алгоритм: Два указателя (с начала и конца)
Действие:

Меняет местами элементы от краев к центру
Эффективно работает за O(n/2)
void print_list(const StringList *list)
Назначение: Вывод списка в консоль
Формат: "Длина списка: X Элементы: elem1 elem2 ..."

void free_list(StringList *list)
Назначение: Освобождение всей памяти списка
Действие:

Освобождает каждую строку в массиве
Освобождает массив указателей
Сбрасывает size и capacity
Функции парсинга (main.c)
void parse_line(const char *line, StringList *list)
Назначение: Разбор строки на токены
Алгоритм:

Пропускает пробелы
Проверяет специальные символы (>>, |, && и др.)
Извлекает простые слова (буквы, цифры, символы)
Добавляет токены в список
int is_special_word(const char *str, int len)
Назначение: Проверка是否是特殊ный символ
Возвращает: 1 если специальный, 0 если нет

int get_special_word_length(const char *str)
Назначение: Определение длины специального символа
Возвращает: Максимальную длину специального символа с начала строки

int is_simple_word_char(char c)
Назначение: Проверка символа простого слова
Возвращает: 1 если символ принадлежит простому слову

🏗 Структуры данных
typedef struct {
    char **items;       // Динамический массив строк
    int size;           // Текущее количество элементов
    int capacity;       // Текущая емкость массива
} StringList;
Простая утилита для работы со списками строк в C.
